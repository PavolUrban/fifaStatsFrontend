import { GoalsByTeamsCountPipe } from './goals-by-teams-count.pipe';

describe('GoalsByTeamsCountPipe', () => {
  it('create an instance', () => {
    const pipe = new GoalsByTeamsCountPipe();
    expect(pipe).toBeTruthy();
  });
});
